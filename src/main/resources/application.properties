spring.application.name=aoty

# where to send the browser after a successful OAuth login — the Vite dev server in local dev,
# should be overridden (e.g. via application-production.properties) once the frontend is deployed
app.frontend-url=http://127.0.0.1:5173

spring.datasource.url=jdbc:postgresql://localhost:5432/aoty
spring.datasource.username=postgres
spring.datasource.password=password

# change to =validate for prod
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

spring.security.oauth2.client.registration.spotify.client-id=${SPOTIFY_CLIENT_ID}
spring.security.oauth2.client.registration.spotify.client-secret=${SPOTIFY_CLIENT_SECRET}
spring.security.oauth2.client.registration.spotify.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.spotify.redirect-uri=http://127.0.0.1:8080/login/oauth2/code/spotify
spring.security.oauth2.client.registration.spotify.scope=user-read-email,user-read-recently-played,playlist-modify-private,ugc-image-upload

spring.security.oauth2.client.provider.spotify.authorization-uri=https://accounts.spotify.com/authorize
spring.security.oauth2.client.provider.spotify.token-uri=https://accounts.spotify.com/api/token
spring.security.oauth2.client.provider.spotify.user-info-uri=https://api.spotify.com/v1/me
spring.security.oauth2.client.provider.spotify.user-name-attribute=account_id